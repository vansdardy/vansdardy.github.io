# Jekyll build output & caches
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# Bundler (if you ever install gems locally into the repo)
vendor/
.bundle/

# Local-only agent guidance — not for the public repo
CLAUDE.md

# OS / editor cruft
.DS_Store
Thumbs.db
